<h1>All Tasks</h1>

<% @tasks.each do |task| %>
<li><a href="/tasks/<%=task.id%>"><%= task.title %></a></li>
<% end %>



<%= link_to "Create a new task", tasks_new_path(@task) %>





<%# now we have a list of tasks, we need to
click on the task title and navigate to a new page,
displaying the details of this task
add a new route, a new controller action and
a new view
this action should find a specific task-thanks to its id
directly from the params

---use the link_to rails helper%>
